#!/usr/bin/env python3
from __future__ import annotations

import os
import subprocess
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[3]
LIB = ROOT / "components/upstream-thin/lib"


def main() -> int:
    commands = {"assemble-full", "verify-full", "qualify-full", "observe-astral", "test"}
    if len(sys.argv) < 2 or sys.argv[1] not in commands:
        print("usage: cpython-android-upstream-thin {assemble-full|verify-full|qualify-full|observe-astral|test} ...", file=sys.stderr)
        return 2
    command = sys.argv[1]
    if command == "test":
        return subprocess.call([sys.executable, "-m", "unittest", "discover", "-s", str(ROOT / "components/upstream-thin/tests"), "-v"], cwd=ROOT)
    implementations = {
        "assemble-full": "assemble_full.py",
        "verify-full": "verify_full.py",
        "qualify-full": "qualify_full.py",
        "observe-astral": "observe_astral.py",
    }
    implementation = LIB / implementations[command]
    env = os.environ.copy()
    env["PYTHONPATH"] = str(LIB) + (os.pathsep + env["PYTHONPATH"] if env.get("PYTHONPATH") else "")
    return subprocess.call([sys.executable, str(implementation), *sys.argv[2:]], cwd=ROOT, env=env)


if __name__ == "__main__":
    raise SystemExit(main())
