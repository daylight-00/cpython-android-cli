# Stage 3-D Gate 3 System-Python Integration Contract Result

> **Status:** CONTRACT FROZEN

The exact supported selector is an absolute installed interpreter path. `uv python find` and `uv venv` are accepted with downloads and managed fallback disabled and with exact selected-realpath and Python metadata verification.

Install-directory and unique executable-name requests are bounded secondary forms. Minor-version PATH, `.python-version`, and `requires-python` behavior is conditional and not patch-exact when both products are exposed. `uv run` and `uv sync` are explicitly deferred to Gate 4 target validation because Gate 2 recorded zero executions for both commands.

No global links, shell changes, uv-managed registration, uv patch, registry migration, journal migration, or product mutation is authorized.
