# Stage 3-F Gate 4 v1 Derivation Failure

> **Status:** PRESERVED EXPECTED FAILURE — authority retention gap

```text
work id          20260716-stage3f-gate4-termux-loopback-actual-acquisition-v1
result sha256    2a076288652f1c342da49eccbe4507291df05d1d596b5c6f1d5646610b5990be
result size      3484
derive rc        1
target rc        not run
repository       unchanged or rolled back
```

The attempt assumed that the transient managed-download archives whose hashes were recorded at Gate 2 could be regenerated byte-for-byte from the same runtime-base payload. That assumption was not frozen or proved. The wrapper rejected the mismatch before target acquisition, preserved complete failure evidence, and left the repository and protected state unchanged.

This failure does not invalidate the source runtime payloads or the Gate 2 canonicalization behavior. It establishes that compressed archive bytes are a separate retained artifact authority and may not be reconstructed from payload equivalence alone.
