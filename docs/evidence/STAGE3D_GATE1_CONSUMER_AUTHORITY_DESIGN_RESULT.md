# Stage 3-D Gate 1 Consumer Authority Design Result

> **Status:** DESIGN FROZEN

Gate 1 opens Stage 3-D without asserting target behavior. It selects uv **system Python** integration as the primary line of inquiry, retains the explicit absolute interpreter path as the canonical control, and defers managed-Python emulation.

Frozen matrix:

```text
explicit-path controls          8
natural discovery              32
project selection               8
precedence and negative        12
transition continuity           4
                               --
total                          64
```

The next target action is a read-only Termux census. It must disable Python downloads, preserve complete PASS-or-FAIL evidence, and avoid global links, uv managed-install registration, shell configuration changes, and canonical product mutations.

This result does not prove that uv naturally discovers either product on Termux.
