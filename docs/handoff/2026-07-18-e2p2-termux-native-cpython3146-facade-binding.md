# Handoff — E2-P2 Termux-native CPython 3.14.6 façade binding

The producer binding is frozen on `main`. Future work continues only on local `main` and `origin/main`.

The stable façade now routes `build` to exact private-authority acquisition and runtime-base plus development-addon reconstruction. `package` consumes that reconstructed prefix under the `termux` host role. The test addon remains preserved but excluded from package input.

Do not modify the historical Gate 1 verifier to make it pass under the new state. Its exact current-state failures are intentionally adjudicated by the binding verifier.

The next bounded transaction is real stable façade build/package execution and independent E2-P1 envelope review. Do not combine it with E2-P3 qualification, selection, publication, installer conversion, or version-transition behavior.
